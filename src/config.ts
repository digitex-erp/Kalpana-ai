// src/config.ts

// This file is now simpler as provider settings are managed dynamically.

// The URL for the Vercel function that proxies requests to the Gemini API.
export const PROXY_URL = '/api/gemini';
